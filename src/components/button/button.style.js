import { COLORS, FONT_SIZE } from "../../constants/theme.js";

export const styles = {

    btn: {
        backgroundColor: COLORS.blue,
        borderRadius: 6,
        padding: 12
    },

    text: {
        color: "#fff",
        fontSize: FONT_SIZE.md,
        textAlign: "center"
    }

}