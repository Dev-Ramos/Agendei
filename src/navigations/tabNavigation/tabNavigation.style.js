import { COLORS, FONT_SIZE } from "../../constants/theme";

export const styles= {
  title: {
    fontSize: FONT_SIZE.xl,
    color: COLORS.blue,
    marginTop: 12,
    marginBottom: 20,
    textAlign: 'center',
    fontWeight: 600,
  },
}